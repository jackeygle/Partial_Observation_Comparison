"""Senseiver: encode sparse sensor readings into a latent, then decode the full
field at queried coordinates.

  sensors / positional   sensor tokens and positional encoding
  network / model        the Senseiver network
  losses / train         loss and training entry point
"""
